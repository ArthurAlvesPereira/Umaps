// MapController.js
export const calcularRota = (local) => {
    // A função pode receber os dados do local e processar a rota
    console.log(`Calculando rota para: ${local.nome}`);
    
    // Aqui você pode adicionar a lógica de cálculo de rota usando algum serviço de mapas (ex: Google Maps API)
    // Simular uma resposta:
    return `Rota calculada para ${local.nome}`;
  };
  